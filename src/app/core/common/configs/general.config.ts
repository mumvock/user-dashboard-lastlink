/**
 * DebounceTime para input de valores que disparam operações no
 * front-end (como filtragem no front-end).
 */
export const FRONT_INPUT_DEBOUNCE_TIME = 150;

/** Mínimo de caracteres digitados pelo usuário para filtro funcionar. */
export const MIN_SEARCH_LENGTH = 1;

/** Tempo estimado – por tanto impreciso – da renderização dinâmica dos accordions. */
export const ACCORDION_RENDER_TIME = 300;
